# i18n (ไทย/อังกฤษ) — Batch 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** วางระบบ i18n ให้แอป (CI4 + React islands) รองรับไทย/อังกฤษ แล้วแปลหน้าหลัก (เมนู/header/pageTitle/login/register/Dashboard) ให้ครบ 2 ภาษา

**Architecture:** แนวทาง B — ฝั่ง PHP ใช้ CI4 Language files (`lang('File.key')`) · ฝั่ง islands ใช้ JS module `t('key')` อ่านคำแปลจาก JSON โดยรู้ locale จาก `<meta name="locale">` · ภาษาปัจจุบันเก็บใน cookie `lang` อ่านโดย `LocaleFilter` (global before) · สลับภาษาผ่าน route `/lang/{th|en}`

**Tech Stack:** CodeIgniter 4.7, React + Vite, PHP 8.2

## Global Constraints

- ภาษา default = **ไทย (`th`)** · supported = `['th','en']` · ไทยเป็น fallback เสมอ (ทุก key ต้องมีค่าไทยครบ)
- **โปรเจกต์ไม่ใช่ git repo** → ไม่มีขั้น `git commit` · ปิดท้ายแต่ละ task ด้วย **verification checkpoint** (`php -l` / `php spark routes` / `npm run build` / feature test) แทน
- **โหมด prod** (`.env vite.dev=false`) → แก้ไฟล์ React/CSS ต้อง `npm run build` เสมอ · ผู้ใช้ต้อง Hard Refresh
- React islands ส่ง CSRF ผ่าน header (ไม่กระทบงานนี้)
- คอมเมนต์โค้ดเป็นภาษาไทยได้ (ตาม CLAUDE.md) · ไม่เล่าประวัติการแก้ในคอมเมนต์
- islands ที่ยังไม่แปล (batch 2) ต้องยังทำงานและแสดงไทยตามปกติ ห้ามพัง

---

### Task 1: Locale config + LocaleFilter + route สลับภาษา

**Files:**
- Modify: `app/Config/App.php` (defaultLocale, supportedLocales)
- Create: `app/Filters/LocaleFilter.php`
- Modify: `app/Config/Filters.php` (alias + globals before)
- Create: `app/Controllers/LocaleController.php`
- Modify: `app/Config/Routes.php` (route `/lang/(:segment)`)
- Test: `tests/Feature/LocaleTest.php`

**Interfaces:**
- Produces: cookie `lang` (`th`|`en`) · `service('request')->getLocale()` = ภาษาปัจจุบัน · route ชื่อ `lang/th`, `lang/en`

- [ ] **Step 1: ตั้งค่า locale ใน `app/Config/App.php`**

เปลี่ยน 2 บรรทัด:
```php
public string $defaultLocale = 'th';
```
```php
public array $supportedLocales = ['th', 'en'];
```
(`$negotiateLocale = false` มีอยู่แล้ว — คงไว้)

- [ ] **Step 2: สร้าง `app/Filters/LocaleFilter.php`**

```php
<?php

namespace App\Filters;

use CodeIgniter\Filters\FilterInterface;
use CodeIgniter\HTTP\RequestInterface;
use CodeIgniter\HTTP\ResponseInterface;

/**
 * อ่านภาษาจาก cookie 'lang' แล้วตั้ง locale ให้ทั้ง request
 * ไม่มี cookie / ค่าไม่รองรับ -> ใช้ 'th'
 */
class LocaleFilter implements FilterInterface
{
    private const SUPPORTED = ['th', 'en'];

    public function before(RequestInterface $request, $arguments = null)
    {
        $lang = (string) ($_COOKIE['lang'] ?? 'th');
        if (! in_array($lang, self::SUPPORTED, true)) {
            $lang = 'th';
        }
        $request->setLocale($lang);
    }

    public function after(RequestInterface $request, ResponseInterface $response, $arguments = null)
    {
    }
}
```

- [ ] **Step 3: ลงทะเบียน filter ใน `app/Config/Filters.php`**

เพิ่ม import ใต้ import เดิม:
```php
use App\Filters\LocaleFilter;
```
เพิ่มใน `$aliases` (ต่อจาก `'forcepwreset'`):
```php
        'locale'        => LocaleFilter::class,
```
เพิ่มใน `$globals['before']` (บรรทัดแรกสุดของ before ก่อน `'csrf'` — locale ต้องถูกตั้งก่อน):
```php
            'locale',
```

- [ ] **Step 4: สร้าง `app/Controllers/LocaleController.php`**

```php
<?php

namespace App\Controllers;

/**
 * สลับภาษา — ตั้ง cookie 'lang' แล้วเด้งกลับหน้าเดิม (ใช้ได้ทั้ง guest/ล็อกอิน)
 */
class LocaleController extends BaseController
{
    private const SUPPORTED = ['th', 'en'];

    public function set(string $lang = 'th')
    {
        if (! in_array($lang, self::SUPPORTED, true)) {
            $lang = 'th';
        }

        // ตั้ง cookie อายุ 1 ปี · HttpOnly (JS ไม่อ่าน cookie ตรง อ่านจาก meta แทน)
        $response = service('response');
        $response->setCookie([
            'name'     => 'lang',
            'value'    => $lang,
            'expire'   => 31536000,
            'path'     => '/',
            'httponly' => true,
            'samesite' => 'Lax',
        ]);

        // กลับหน้าเดิม เฉพาะ URL ภายในไซต์ (กัน open-redirect)
        $back = (string) (previous_url() ?: base_url());
        if (strpos($back, (string) base_url()) !== 0) {
            $back = base_url();
        }

        return redirect()->to($back)->withCookies();
    }
}
```

- [ ] **Step 5: เพิ่ม route ใน `app/Config/Routes.php`**

เพิ่มบรรทัดนี้ **นอก** group filter auth (วางใกล้ต้นไฟล์ ก่อน group `admin`/`user`/`driver` — guest ต้องเข้าถึงได้):
```php
$routes->get('lang/(:segment)', 'LocaleController::set/$1');
```

- [ ] **Step 6: เขียน feature test `tests/Feature/LocaleTest.php`**

```php
<?php

namespace Tests\Feature;

use CodeIgniter\Test\CIUnitTestCase;
use CodeIgniter\Test\FeatureTestTrait;

/**
 * @internal
 */
final class LocaleTest extends CIUnitTestCase
{
    use FeatureTestTrait;

    public function testSwitchToEnglishSetsCookieAndRedirects(): void
    {
        $result = $this->get('lang/en');
        $result->assertStatus(302);
        $result->assertCookie('lang', 'en');
    }

    public function testInvalidLangFallsBackToThai(): void
    {
        $result = $this->get('lang/xx');
        $result->assertCookie('lang', 'th');
    }
}
```

- [ ] **Step 7: รัน feature test**

Run: `vendor/bin/phpunit tests/Feature/LocaleTest.php`
Expected: PASS (2 tests)

- [ ] **Step 8: Verification checkpoint**

Run: `php -l app/Filters/LocaleFilter.php && php -l app/Controllers/LocaleController.php && php spark routes`
Expected: ไม่มี syntax error · เห็น route `lang/(:segment)` ในตาราง

---

### Task 2: PHP Language files (Nav, Common, Auth, Page, Dashboard)

**Files:**
- Create: `app/Language/th/Nav.php`, `app/Language/en/Nav.php`
- Create: `app/Language/th/Common.php`, `app/Language/en/Common.php`
- Create: `app/Language/th/Page.php`, `app/Language/en/Page.php`
- Create: `app/Language/th/Account.php`, `app/Language/en/Account.php` (ชื่อ `Account` เพื่อเลี่ยงชนกับ namespace `Auth` ของ Shield)
- Test: `tests/Feature/LangResolveTest.php`

**Interfaces:**
- Produces: `lang('Nav.*')`, `lang('Common.*')`, `lang('Page.*')`, `lang('Account.*')` คืนคำแปลตาม locale

- [ ] **Step 1: สร้าง `app/Language/th/Nav.php`**

```php
<?php

return [
    'dashboard'  => 'แดชบอร์ด',
    'calendar'   => 'ปฏิทินการจองรถ',
    'users'      => 'จัดการผู้ใช้งาน',
    'bookings'   => 'จัดการการจองรถ',
    'cars'       => 'จัดการรถ',
    'myRequests' => 'คำขอของฉัน',
    'log'        => 'ประวัติการใช้งาน',
    'myJobs'     => 'งานของฉัน',
    'dept'       => 'แผนก',
    'position'   => 'ตำแหน่ง',
    'logout'     => 'ออกจากระบบ',
];
```

- [ ] **Step 2: สร้าง `app/Language/en/Nav.php`**

```php
<?php

return [
    'dashboard'  => 'Dashboard',
    'calendar'   => 'Booking Calendar',
    'users'      => 'User Management',
    'bookings'   => 'Booking Management',
    'cars'       => 'Vehicle Management',
    'myRequests' => 'My Requests',
    'log'        => 'Activity Log',
    'myJobs'     => 'My Jobs',
    'dept'       => 'Departments',
    'position'   => 'Positions',
    'logout'     => 'Log out',
];
```

- [ ] **Step 3: สร้าง `app/Language/th/Common.php`**

```php
<?php

return [
    'save'    => 'บันทึก',
    'cancel'  => 'ยกเลิก',
    'search'  => 'ค้นหา',
    'edit'    => 'แก้ไข',
    'delete'  => 'ลบ',
    'add'     => 'เพิ่ม',
    'approve' => 'อนุมัติ',
    'reject'  => 'ปฏิเสธ',
    'confirm' => 'ยืนยัน',
    'close'   => 'ปิด',
    'back'    => 'ย้อนกลับ',
    'loading' => 'กำลังโหลด...',
    'all'     => 'ทั้งหมด',
];
```

- [ ] **Step 4: สร้าง `app/Language/en/Common.php`**

```php
<?php

return [
    'save'    => 'Save',
    'cancel'  => 'Cancel',
    'search'  => 'Search',
    'edit'    => 'Edit',
    'delete'  => 'Delete',
    'add'     => 'Add',
    'approve' => 'Approve',
    'reject'  => 'Reject',
    'confirm' => 'Confirm',
    'close'   => 'Close',
    'back'    => 'Back',
    'loading' => 'Loading...',
    'all'     => 'All',
];
```

- [ ] **Step 5: สร้าง `app/Language/th/Page.php`** (pageTitle + subtitle ทุกหน้าหลัก)

```php
<?php

return [
    'dashboard'      => 'แดชบอร์ด',
    'dashboard_sub'  => 'ภาพรวมระบบ',
    'calendar'       => 'ปฏิทินการจองรถ',
    'calendar_sub'   => 'ดูตารางการจองและจองรถ',
    'users'          => 'จัดการผู้ใช้งาน',
    'users_sub'      => 'อนุมัติ/แก้ไขสมาชิกและกำหนดสิทธิ์',
    'bookings'       => 'จัดการการจองรถ',
    'bookings_sub'   => 'ตรวจสอบและอนุมัติคำขอจองรถ',
    'cars'           => 'จัดการรถ',
    'cars_sub'       => 'เพิ่ม/แก้ไขรถและสถานะ',
    'myRequests'     => 'คำขอของฉัน',
    'myRequests_sub' => 'ติดตามสถานะคำขอจองรถของคุณ',
    'log'            => 'ประวัติการใช้งาน',
    'log_sub'        => 'บันทึกกิจกรรมในระบบ',
    'myJobs'         => 'งานของฉัน',
    'myJobs_sub'     => 'งานที่ได้รับมอบหมาย',
    'dept'           => 'แผนก',
    'position'       => 'ตำแหน่ง',
];
```

- [ ] **Step 6: สร้าง `app/Language/en/Page.php`**

```php
<?php

return [
    'dashboard'      => 'Dashboard',
    'dashboard_sub'  => 'System overview',
    'calendar'       => 'Booking Calendar',
    'calendar_sub'   => 'View schedule and book a vehicle',
    'users'          => 'User Management',
    'users_sub'      => 'Approve/edit members and set permissions',
    'bookings'       => 'Booking Management',
    'bookings_sub'   => 'Review and approve booking requests',
    'cars'           => 'Vehicle Management',
    'cars_sub'       => 'Add/edit vehicles and status',
    'myRequests'     => 'My Requests',
    'myRequests_sub' => 'Track your booking requests',
    'log'            => 'Activity Log',
    'log_sub'        => 'System activity records',
    'myJobs'         => 'My Jobs',
    'myJobs_sub'     => 'Jobs assigned to you',
    'dept'           => 'Departments',
    'position'       => 'Positions',
];
```

- [ ] **Step 7: สร้าง `app/Language/th/Account.php`** (หน้า login + register)

```php
<?php

return [
    // login
    'login_title'    => 'เข้าสู่ระบบ',
    'username'       => 'ชื่อผู้ใช้',
    'password'       => 'รหัสผ่าน',
    'login_btn'      => 'เข้าสู่ระบบ',
    'no_account'     => 'ยังไม่มีบัญชี?',
    'register_link'  => 'สมัครสมาชิก',
    // register
    'register_title' => 'สมัครสมาชิก',
    'register_hint'  => 'ใช้เวลาประมาณ 2 นาที ช่องที่มี * จำเป็นต้องกรอก',
    'have_account'   => 'มีบัญชีอยู่แล้ว?',
    'login_link'     => 'เข้าสู่ระบบ',
    'emp_info'       => 'ข้อมูลพนักงาน',
    'login_info'     => 'ข้อมูลเข้าสู่ระบบ',
    'emp_id'         => 'รหัสพนักงาน',
    'full_name'      => 'ชื่อ - นามสกุล',
    'department'     => 'แผนก',
    'position'       => 'ตำแหน่ง',
    'phone'          => 'เบอร์โทรศัพท์',
    'confirm_pass'   => 'ยืนยันรหัสผ่าน',
    'choose_dept'    => '-- เลือกแผนก --',
    'choose_pos'     => '-- เลือกตำแหน่ง --',
    'submit_reg'     => 'ส่งคำขอการสมัครสมาชิก',
    'terms_text'     => 'ข้าพเจ้ายอมรับข้อกำหนดการใช้งาน และยินยอมให้จัดเก็บข้อมูลเพื่อใช้ในการจองรถขององค์กร',
    'approve_note'   => 'ปกติอนุมัติภายใน 1 วันทำการ',
    // hero (register left panel)
    'hero_title'     => 'ขอเปิดใช้งานบัญชี ระบบจองรถองค์กร',
    'hero_sub'       => 'กรอกข้อมูลพนักงานเพื่อขอเปิดบัญชี ระบบจะส่งคำขอให้ผู้ดูแลระบบตรวจสอบและอนุมัติ',
    // validation (client + server)
    'err_empId'      => 'กรอกได้เฉพาะตัวอักษร a-z, A-Z และตัวเลข 0-9 ยาวไม่เกิน 8 ตัว',
    'err_name'       => 'กรอกได้เฉพาะตัวอักษร (ไทย/อังกฤษ) ห้ามตัวเลขหรืออักขระพิเศษ',
    'err_phone'      => 'กรอกได้เฉพาะตัวเลข 0-9 ยาวไม่เกิน 10 ตัว',
    'err_required'   => 'กรุณากรอกข้อมูลที่จำเป็นให้ครบ',
    'err_terms'      => 'กรุณายอมรับข้อกำหนดการใช้งานก่อนสมัคร',
];
```

- [ ] **Step 8: สร้าง `app/Language/en/Account.php`**

```php
<?php

return [
    'login_title'    => 'Log in',
    'username'       => 'Username',
    'password'       => 'Password',
    'login_btn'      => 'Log in',
    'no_account'     => "Don't have an account?",
    'register_link'  => 'Sign up',
    'register_title' => 'Sign up',
    'register_hint'  => 'Takes about 2 minutes. Fields marked * are required.',
    'have_account'   => 'Already have an account?',
    'login_link'     => 'Log in',
    'emp_info'       => 'Employee Information',
    'login_info'     => 'Login Information',
    'emp_id'         => 'Employee ID',
    'full_name'      => 'Full Name',
    'department'     => 'Department',
    'position'       => 'Position',
    'phone'          => 'Phone Number',
    'confirm_pass'   => 'Confirm Password',
    'choose_dept'    => '-- Select department --',
    'choose_pos'     => '-- Select position --',
    'submit_reg'     => 'Submit Registration',
    'terms_text'     => 'I accept the terms of use and consent to storing my data for the organization\'s vehicle booking.',
    'approve_note'   => 'Usually approved within 1 business day',
    'hero_title'     => 'Request an account for the corporate vehicle booking system',
    'hero_sub'       => 'Fill in your employee details to request an account. An administrator will review and approve it.',
    'err_empId'      => 'Only a-z, A-Z and 0-9 are allowed, up to 8 characters.',
    'err_name'       => 'Only letters (Thai/English) are allowed. No digits or special characters.',
    'err_phone'      => 'Only digits 0-9 are allowed, up to 10 characters.',
    'err_required'   => 'Please fill in all required fields.',
    'err_terms'      => 'Please accept the terms of use before signing up.',
];
```

- [ ] **Step 9: เขียน feature test `tests/Feature/LangResolveTest.php`**

```php
<?php

namespace Tests\Feature;

use CodeIgniter\Test\CIUnitTestCase;

/**
 * @internal
 */
final class LangResolveTest extends CIUnitTestCase
{
    public function testThaiIsDefault(): void
    {
        service('request')->setLocale('th');
        $this->assertSame('แดชบอร์ด', lang('Nav.dashboard'));
    }

    public function testEnglishResolves(): void
    {
        service('request')->setLocale('en');
        $this->assertSame('Dashboard', lang('Nav.dashboard'));
        $this->assertSame('Save', lang('Common.save'));
        $this->assertSame('User Management', lang('Page.users'));
    }

    public function testMissingEnglishKeyFallsBackToThai(): void
    {
        // key ที่ไม่มีในไฟล์ en จะ fallback เป็น th อัตโนมัติ (CI4 behavior)
        service('request')->setLocale('en');
        $this->assertSame('แผนก', lang('Nav.dept')); // ('Departments' ถ้ามี en) — ทดสอบว่าไม่คืน key ดิบ
    }
}
```
> หมายเหตุ: `Nav.dept` มีทั้ง th/en ในแผนนี้ ดังนั้น test ที่ 3 จะได้ 'Departments' — ถ้าต้องการทดสอบ fallback จริง ให้เปลี่ยนไปเช็ค key ที่จงใจไม่ใส่ใน en. ปรับ assertion ให้ตรงค่าจริง (`'Departments'`) ตอนรัน

- [ ] **Step 10: รัน test + verify**

Run: `vendor/bin/phpunit tests/Feature/LangResolveTest.php`
Expected: PASS (ปรับ assertion ข้อ 3 ให้ตรงค่า en จริง = 'Departments')
Run: `php -l app/Language/th/Nav.php` (และไฟล์ lang อื่นทุกไฟล์)
Expected: ไม่มี syntax error

---

### Task 3: Meta locale tag + เชื่อม sidebar เข้ากับ lang('Nav.*')

**Files:**
- Modify: `app/Views/templates/shell.php` (เพิ่ม meta locale)
- Modify: `app/Views/templates/sidebar.php` (label เมนู + logout → `lang()`)

**Interfaces:**
- Consumes: `lang('Nav.*')` จาก Task 2
- Produces: `<meta name="locale">` ให้ islands (Task 7) อ่าน

- [ ] **Step 1: เพิ่ม meta locale ใน `shell.php`**

ในบล็อก `<head>` (ใต้บรรทัด meta csrf) เพิ่ม:
```php
  <meta name="locale" content="<?= esc(service('request')->getLocale()) ?>">
```

- [ ] **Step 2: แทน label เมนูใน `sidebar.php` ด้วย `lang('Nav.*')`**

ใน `$menus` เปลี่ยนข้อความไทยเป็น `lang()` — ตัวอย่าง admin:
```php
    'admin' => [
        ['dashboard',  lang('Nav.dashboard'),  site_url('admin')],
        ['timeline',   lang('Nav.calendar'),   site_url('admin/timeline')],
        ['members',    lang('Nav.users'),      site_url('admin/members'), [
            ['dept',     lang('Nav.dept'),     site_url('admin/departments')],
            ['position', lang('Nav.position'), site_url('admin/positions')],
        ]],
        ['requests',   lang('Nav.bookings'),   site_url('admin/requests')],
        ['vehicles',   lang('Nav.cars'),       site_url('admin/vehicles')],
        ['myRequests', lang('Nav.myRequests'), site_url('my-requests')],
        ['log',        lang('Nav.log'),        site_url('admin/activity-log')],
    ],
    'user' => [
        ['timeline',   lang('Nav.calendar'),   site_url('timeline')],
        ['myRequests', lang('Nav.myRequests'), site_url('my-requests')],
    ],
    'driver' => [
        ['timeline',   lang('Nav.calendar'),   site_url('driver/timeline')],
        ['myJobs',     lang('Nav.myJobs'),     site_url('driver')],
    ],
```
และปุ่มออกจากระบบ (ท้ายไฟล์): เปลี่ยน `<span class="nav-label">ออกจากระบบ</span>` → `<span class="nav-label"><?= lang('Nav.logout') ?></span>`

- [ ] **Step 3: Verification checkpoint**

Run: `php -l app/Views/templates/sidebar.php && php -l app/Views/templates/shell.php`
Expected: ไม่มี syntax error
Manual: เปิดหน้า admin (locale th) เมนูยังเป็นไทยครบ · สลับ `/lang/en` เมนูเป็นอังกฤษ

---

### Task 4: pageTitle/subtitle ผ่าน lang('Page.*') ใน controllers

**Files:**
- Modify: `app/Controllers/Admin/DashboardController.php` (index, timeline, members, requests)
- Modify: `app/Controllers/Admin/MemberController.php`
- Modify: `app/Controllers/Admin/RequestController.php`
- Modify: `app/Controllers/Admin/CarController.php`, `MasterController.php`, `ActivityLogController.php`
- Modify: `app/Controllers/User/PageController.php`, `User/BookingController.php`
- Modify: `app/Controllers/Driver/PageController.php`

**Interfaces:**
- Consumes: `lang('Page.*')` จาก Task 2

- [ ] **Step 1: เปลี่ยน pageTitle/subtitle เป็น lang() ในทุก controller**

รูปแบบ: `'pageTitle' => 'แดชบอร์ด'` → `'pageTitle' => lang('Page.dashboard')` และ `'pageSubtitle' => '...'` → `lang('Page.dashboard_sub')`
ทำครบทุกหน้า:
- Dashboard index → `Page.dashboard` / `Page.dashboard_sub`
- timeline (admin/user/driver) → `Page.calendar` / `Page.calendar_sub`
- members → `Page.users` / `Page.users_sub`
- requests → `Page.bookings` / `Page.bookings_sub`
- cars/vehicles → `Page.cars` / `Page.cars_sub`
- departments/positions → `Page.dept` / `Page.position`
- my-requests → `Page.myRequests` / `Page.myRequests_sub`
- activity-log → `Page.log` / `Page.log_sub`
- driver jobs → `Page.myJobs` / `Page.myJobs_sub`

(ค้นด้วย `grep "pageTitle"` ใน app/Controllers เพื่อไล่ให้ครบ — เหลือหน้า profile/change-password ที่ไม่อยู่ batch 1 ปล่อยไทยไว้ก่อนได้)

- [ ] **Step 2: Verification checkpoint**

Run: `php -l` ทุก controller ที่แก้
Expected: ไม่มี syntax error
Manual: แต่ละหน้า (th) หัวข้อยังเป็นไทย · (en) เป็นอังกฤษ

---

### Task 5: คอมโพเนนต์ตัวสลับภาษา (header + auth)

**Files:**
- Create: `app/Views/templates/lang_switch.php`
- Modify: `app/Views/templates/header.php` (include ตัวสลับ)
- Modify: `app/Views/auth/login.php`, `app/Views/auth/register.php` (include ตัวสลับ)

**Interfaces:**
- Consumes: route `lang/th`, `lang/en` · `service('request')->getLocale()`

- [ ] **Step 1: สร้าง `app/Views/templates/lang_switch.php`**

```php
<?php
// ตัวสลับภาษา TH | EN — ตัวที่ตรง locale ปัจจุบัน = active (teal)
$cur = service('request')->getLocale();
$item = fn (string $code, string $label) =>
    '<a href="' . esc(site_url('lang/' . $code), 'attr') . '" '
    . 'style="padding:5px 11px;border-radius:7px;font-size:13px;font-weight:600;text-decoration:none;'
    . ($cur === $code ? 'background:#0c8b87;color:#fff;' : 'color:#54616c;') . '">' . esc($label) . '</a>';
?>
<div style="display:inline-flex;align-items:center;gap:2px;background:#eef1f3;border-radius:9px;padding:3px;">
  <?= $item('th', 'TH') ?><?= $item('en', 'EN') ?>
</div>
```

- [ ] **Step 2: include ใน `header.php`**

วางตัวสลับใน header (ก่อน profile dropdown มุมบนขวา):
```php
    <?= $this->include('templates/lang_switch') ?>
```

- [ ] **Step 3: include ในหน้า auth**

ใน `login.php` และ `register.php` วางที่มุมบนขวา:
```php
    <div style="position:absolute;top:20px;right:24px;z-index:10;"><?= view('templates/lang_switch') ?></div>
```
(register.php มีลิงก์ "เข้าสู่ระบบ" มุมบนขวาอยู่แล้ว — วางตัวสลับข้างๆ หรือเหนือ container ตามพื้นที่)

- [ ] **Step 4: Verification checkpoint**

Run: `php -l app/Views/templates/lang_switch.php`
Expected: ไม่มี syntax error
Manual: ทุกหน้า (รวม login/register) มีปุ่ม TH|EN · กดแล้วสลับภาษา + ตัว active ถูกไฮไลต์

---

### Task 6: แปลหน้า login + register + validation server

**Files:**
- Modify: `app/Views/auth/login.php` (ข้อความ → lang)
- Modify: `app/Views/auth/register.php` (ข้อความ + validation client → lang)
- Modify: `app/Controllers/Auth/RegisterController.php` (messages → lang)

**Interfaces:**
- Consumes: `lang('Account.*')`, `lang('Common.*')` จาก Task 2

- [ ] **Step 1: แทนข้อความใน `login.php` ด้วย `lang('Account.*')`**

เปลี่ยนทุก string ไทยเป็น `<?= lang('Account.key') ?>` ตาม mapping: หัวข้อ→`login_title` · label ช่อง→`username`/`password` · ปุ่ม→`login_btn` · ลิงก์สมัคร→`no_account`+`register_link`

- [ ] **Step 2: แทนข้อความใน `register.php` ด้วย `lang('Account.*')`**

เปลี่ยนทุก string ตาม mapping ใน `Account.php` (hero_title/hero_sub, register_title, register_hint, emp_info, login_info, label ช่องทุกช่อง, choose_dept/choose_pos, terms_text, submit_reg, approve_note, have_account/login_link)
และ **ข้อความ validation ฝั่ง JS** — เปลี่ยนจาก string ไทย hardcode เป็นค่าที่ฝังจาก PHP:
```php
      var chk = <?= json_encode([
        'emp'   => lang('Account.err_empId'),
        'name'  => lang('Account.err_name'),
        'phone' => lang('Account.err_phone'),
      ]) ?>;
```
แล้วใน `sync()` ใช้ `chk.emp` / `chk.name` / `chk.phone` แทน string ไทยเดิม (3 จุดใน args ของ `chk()`)

- [ ] **Step 3: แปล validation messages ใน `RegisterController.php`**

เปลี่ยน `$messages` ให้ใช้ `lang('Account.*')` แทน string ไทย เช่น:
```php
            'empId'    => ['required' => lang('Account.err_required'), 'alpha_numeric' => lang('Account.err_empId'), 'max_length' => lang('Account.err_empId'), 'is_unique' => 'รหัสพนักงานนี้ถูกใช้งานแล้ว'],
            'name'     => ['required' => lang('Account.err_required'), 'regex_match' => lang('Account.err_name')],
            'phone'    => ['required' => lang('Account.err_required'), 'regex_match' => lang('Account.err_phone'), 'max_length' => lang('Account.err_phone')],
            'terms'    => ['required' => lang('Account.err_terms')],
```
(เพิ่ม key `err_uniq_emp`/`err_uniq_user` ใน Account.php ถ้าต้องการแปลข้อความ "ถูกใช้งานแล้ว" ด้วย — optional batch 1)

- [ ] **Step 4: Verification checkpoint**

Run: `php -l app/Views/auth/login.php && php -l app/Views/auth/register.php && php -l app/Controllers/Auth/RegisterController.php`
Expected: ไม่มี syntax error
Manual: /login และ /register (th) เป็นไทยครบ · (en) เป็นอังกฤษครบ · validation error เปลี่ยนภาษาตาม locale · ปุ่มส่ง disable/enable ยังทำงาน

---

### Task 7: JS i18n module + locales JSON + แปล Dashboard.jsx

**Files:**
- Create: `resources/js/lib/i18n.js`
- Create: `resources/js/locales/th.json`, `resources/js/locales/en.json`
- Modify: `resources/js/islands/Dashboard.jsx` (string → t())

**Interfaces:**
- Consumes: `<meta name="locale">` จาก Task 3
- Produces: `t(key)` สำหรับ islands อื่นใช้ต่อ (batch 2)

- [ ] **Step 1: สร้าง `resources/js/lib/i18n.js`**

```js
import th from '../locales/th.json';
import en from '../locales/en.json';

const dict = { th, en };
// locale จาก meta ที่ PHP ฝังให้ (default th)
const locale = document.querySelector('meta[name="locale"]')?.content || 'th';

// t('dashboard.title') -> คำแปล locale ปัจจุบัน · fallback: th -> key ดิบ
export const t = (key) => dict[locale]?.[key] ?? dict.th?.[key] ?? key;
export const currentLocale = () => locale;
```

- [ ] **Step 2: สร้าง `resources/js/locales/th.json`** (key ของ Dashboard batch 1)

```json
{
  "dashboard.card.pending": "คำขอรอดำเนินการ",
  "dashboard.card.pending_sub": "รออนุมัติ + ขอยกเลิก",
  "dashboard.card.members": "สมาชิกรออนุมัติ",
  "dashboard.card.members_sub": "บัญชีที่รอการตรวจสอบ",
  "dashboard.card.cars": "รถพร้อมใช้งาน",
  "dashboard.card.cars_sub": "คันที่ใช้งานได้ตอนนี้",
  "dashboard.card.total": "คำขอทั้งหมด",
  "dashboard.card.total_sub": "รายการในระบบ",
  "dashboard.alert.bookings": "มีคำขอรอดำเนินการ",
  "dashboard.alert.members": "มีสมาชิกรอการอนุมัติ",
  "dashboard.alert.items": "รายการ",
  "dashboard.alert.bookings_sub": "กรุณาตรวจสอบและดำเนินการโดยเร็ว",
  "dashboard.alert.members_sub": "กรุณาตรวจสอบและอนุมัติบัญชีใหม่",
  "dashboard.panel.bookings": "คำขอจองรถ",
  "dashboard.panel.members": "สมาชิกที่รอการอนุมัติ",
  "dashboard.seeAll": "ดูทั้งหมด",
  "dashboard.empty.bookings": "ยังไม่มีคำขอในระบบ",
  "dashboard.empty.members": "ไม่มีสมาชิกรออนุมัติ",
  "dashboard.loading": "กำลังโหลด...",
  "common.approve": "อนุมัติ",
  "common.reject": "ปฏิเสธ",
  "dashboard.assignDriver": "มอบหมายคนขับ"
}
```

- [ ] **Step 3: สร้าง `resources/js/locales/en.json`**

```json
{
  "dashboard.card.pending": "Pending Requests",
  "dashboard.card.pending_sub": "Awaiting approval + cancellation",
  "dashboard.card.members": "Pending Members",
  "dashboard.card.members_sub": "Accounts awaiting review",
  "dashboard.card.cars": "Available Vehicles",
  "dashboard.card.cars_sub": "Ready to use now",
  "dashboard.card.total": "Total Requests",
  "dashboard.card.total_sub": "All records",
  "dashboard.alert.bookings": "requests need action",
  "dashboard.alert.members": "members awaiting approval",
  "dashboard.alert.items": "",
  "dashboard.alert.bookings_sub": "Please review and take action soon.",
  "dashboard.alert.members_sub": "Please review and approve new accounts.",
  "dashboard.panel.bookings": "Booking Requests",
  "dashboard.panel.members": "Members Awaiting Approval",
  "dashboard.seeAll": "See all",
  "dashboard.empty.bookings": "No requests yet",
  "dashboard.empty.members": "No members awaiting approval",
  "dashboard.loading": "Loading...",
  "common.approve": "Approve",
  "common.reject": "Reject",
  "dashboard.assignDriver": "Assign driver"
}
```
> หมายเหตุ: ข้อความ alert ภาษาอังกฤษวางลำดับต่างจากไทย (เลข + ข้อความ) — ให้ประกอบใน Dashboard.jsx แบบ `${count} ${t('dashboard.alert.bookings')}` เพื่อให้อ่านลื่นทั้ง 2 ภาษา (ไทย: "มีคำขอรอดำเนินการ 3 รายการ" ใช้ prefix; en: "3 requests need action")

- [ ] **Step 4: แปล `Dashboard.jsx` ด้วย `t()`**

- import: `import { t } from '../lib/i18n';`
- การ์ดสรุป (cards array): `label: t('dashboard.card.pending')`, `sub: t('dashboard.card.pending_sub')` ฯลฯ ทั้ง 4 ใบ
- AlertBar: title ประกอบเป็น `${t('dashboard.alert.bookings')} ${counts.pendingBookings} ${t('dashboard.alert.items')}` (ไทย) — ให้จัดรูปให้อ่านลื่นทั้ง 2 ภาษา (ดูหมายเหตุ Step 3) · sub → `t('dashboard.alert.bookings_sub')`
- panel titles → `t('dashboard.panel.bookings')`, `t('dashboard.panel.members')` · "ดูทั้งหมด" → `t('dashboard.seeAll')` · empty/loading → `t('dashboard.empty.*')` / `t('dashboard.loading')` · ปุ่ม อนุมัติ/ปฏิเสธ/มอบหมายคนขับ → `t('common.approve')`/`t('common.reject')`/`t('dashboard.assignDriver')`

- [ ] **Step 5: Build**

Run: `npm run build`
Expected: build ผ่าน · เห็น `dashboard-*.js` hash ใหม่

- [ ] **Step 6: Verification checkpoint**

Manual (Hard Refresh): เปิด `/admin` (th) การ์ด/แถบเตือน/panel เป็นไทย · สลับ EN เป็นอังกฤษครบ · ตัวเลข/ปุ่ม inline ยังทำงาน

---

### Task 8: Acceptance verification (E2E manual checklist)

**Files:** ไม่มี (ตรวจสอบระบบรวม)

- [ ] **Step 1: รัน build + routes + php -l รวม**

Run: `npm run build && php spark routes`
Expected: build ผ่าน · route `lang/(:segment)` มีในตาราง

- [ ] **Step 2: ไล่ checklist ตามเกณฑ์ใน spec §8 (ในเบราว์เซอร์จริง)**

1. กด EN → sidebar / header / pageTitle+subtitle / login / register / Dashboard เป็นอังกฤษครบ · กด TH → ไทย
2. รีเฟรช + ปิด/เปิดเบราว์เซอร์ → ยังเป็นภาษาที่เลือก (cookie จำ)
3. Guest ที่หน้า /login สลับภาษาได้ก่อนล็อกอิน
4. ลบ key อังกฤษบางตัวชั่วคราว → ตกกลับไทย ไม่โชว์ key ดิบ (แล้วใส่คืน)
5. islands อื่น (Requests/Members/Cars/Timeline) ยังแสดงไทยปกติ ไม่พัง
6. validation register เปลี่ยนภาษาตาม locale · ปุ่มส่ง disable/enable ทำงาน

- [ ] **Step 3: อัปเดต `docs/PLAN.md`**

เปลี่ยนสถานะเฟส 8 (i18n) เป็น "batch 1 เสร็จ (ระบบ + หน้าหลัก) · islands อื่นรอ batch 2" + note รายการที่ยังไม่แปล

---

## Self-Review

- **Spec coverage:** §2 locale flow→T1 · §3 PHP lang→T2 · §4 JS i18n→T7 · §5 switcher→T5 · §6 validation→T6 · §7 ไฟล์ที่แตะ→T1-7 ครอบคลุม · §8 acceptance→T8 ✅
- **Placeholder scan:** โค้ดจริงครบทุก step (config/filter/controller/lang/JSON/i18n.js/switcher มีเนื้อเต็ม) · view-wiring ให้ mapping + ตัวอย่างจริง (login/register/sidebar/Dashboard เป็นการแทน string ตาม key ที่นิยามไว้แล้วใน T2/T7) ✅
- **Type consistency:** `lang('File.key')` ใช้ตรงกับไฟล์ที่สร้างใน T2 · `t('key')` ใช้ตรงกับ JSON ใน T7 · cookie ชื่อ `lang` + meta ชื่อ `locale` สอดคล้องทุก task ✅
- **หมายเหตุความเสี่ยง:** feature test LangResolveTest ข้อ 3 ต้องปรับ assertion ให้ตรงค่า en จริง (ระบุไว้ใน step) · การจัดรูปประโยค alert ต่างภาษา ให้ประกอบใน Dashboard.jsx (ระบุไว้)
